# Project Assets Placeholder

Hãy thả các ảnh chụp màn hình (screenshots) hoặc hình động (GIFs) thực tế từ dự án của bạn vào thư mục này để hiển thị trên file README chính:

1. **`homepage_preview.png`**: Ảnh chụp màn hình trang chủ EduStream.
2. **`cart_sidebar_preview.png`**: Ảnh chụp màn hình khi thêm khóa học vào giỏ hàng hoặc xem danh sách bài giảng.
3. **`vietqr_payment_preview.png`**: Ảnh chụp màn hình giao diện quét mã QR thanh toán của SePay.
4. **`ai_tutor_preview.png`**: Ảnh chụp chatbot trợ lý ảo AI Tutor.

*Sau khi thả ảnh vào thư mục này, chỉ cần chạy các lệnh Git sau để đẩy ảnh lên GitHub:*
```bash
git add assets/
git commit -m "Add project showcase preview images"
git push origin main
```
